<configuration xmlns="urn:hornetq"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="urn:hornetq ../../../schemas/hornetq-configuration.xsd">

    <!-- Turned on so can monitor the number of messages added to queues. -->
    <!--<jmx-management-enabled>false</jmx-management-enabled>-->
    <!--<message-counter-enabled>true</message-counter-enabled>-->
    <!--<message-counter-sample-period>2000</message-counter-sample-period>-->
    <!--<message-counter-max-day-history>2</message-counter-max-day-history>-->

    <management-address>hornetq.management</management-address>

    <!-- Location of all of the JMS data. -->
    <bindings-directory>${mvn.broker.bindings.directory}</bindings-directory>
    <journal-directory>${mvn.broker.journal.directory}</journal-directory>
    <large-messages-directory>${mvn.broker.large.messages.directory}</large-messages-directory>
    <paging-directory>${mvn.broker.paging.directory}</paging-directory>
    <!-- Experimenting with 10 second timeout on the client ping.
        (Defaults to five minutes, but the client should ping every 5 seconds.)-->
    <connection-ttl-override>600000</connection-ttl-override>

    <connectors>
        <connector name="netty-connector">
            <factory-class>org.hornetq.integration.transports.netty.NettyConnectorFactory</factory-class>
            <!--param key="hornetq.remoting.netty.host"  value="phildell.windows.ebi.ac.uk" type="String"/-->
            <param key="host"  value="${mvn.broker.host.name}"/>
            <param key="port" value="${mvn.broker.port.number}"/>
        </connector>

    </connectors>

    <acceptors>
        <acceptor name="netty-acceptor">
            <factory-class>org.hornetq.integration.transports.netty.NettyAcceptorFactory</factory-class>
            <!--param key="hornetq.remoting.netty.host"  value="phildell.windows.ebi.ac.uk" type="String"/-->
            <param key="host"  value="${mvn.broker.host.name}"/>
            <param key="port"  value="${mvn.broker.port.number}"/>
        </acceptor>
    </acceptors>

    <security-settings>
        <security-setting match="jms.queue.#">
            <permission type="manage" roles="guest"/>
            <permission type="send" roles="guest"/>
            <permission type="consume" roles="guest"/>
        </security-setting>
    </security-settings>
    
    <security-enabled>false</security-enabled>


</configuration>
