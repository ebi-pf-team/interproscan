NCOILS
======

To run the ncoils binary, you need to make it:

prompt$ make

To execute it (display usage instructions):

prompt$ ./ncoils -h


NCOILSD
=======

To run the ncoilsd daemon, you need to make it:

prompt$ make

To execute it:

prompt$ ./ncoilsd 51717 /opt/run/ncoilsd /opt/log/ncoilsd 

To test it, there is a client in the test directory:

client.pl -file C04A11.3.fasta 

This should print out a per residue score for the query sequence.

